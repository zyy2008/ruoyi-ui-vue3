<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.admin.mapper.TSurveySoilPollutionMapper">
    
    <resultMap type="TSurveySoilPollution" id="TSurveySoilPollutionResult">
        <result property="id"    column="id"    />
        <result property="surveyAssessmentId"    column="survey_assessment_id"    />
        <result property="pollutantName"    column="pollutant_name"    />
        <result property="maxConcentration"    column="max_concentration"    />
        <result property="deptId"    column="dept_id"    />
    </resultMap>

    <sql id="selectTSurveySoilPollutionVo">
        select id, survey_assessment_id, pollutant_name, max_concentration,dept_id from t_survey_soil_pollution
    </sql>

    <select id="selectTSurveySoilPollutionList" parameterType="TSurveySoilPollution" resultMap="TSurveySoilPollutionResult">
        <include refid="selectTSurveySoilPollutionVo"/>
        <where>  
            <if test="surveyAssessmentId != null  and surveyAssessmentId != ''"> and survey_assessment_id = #{surveyAssessmentId}</if>
            <if test="pollutantName != null  and pollutantName != ''"> and pollutant_name like concat('%', #{pollutantName}, '%')</if>
            <if test="maxConcentration != null  and maxConcentration != ''"> and max_concentration = #{maxConcentration}</if>
            <if test="deptId != null  and deptId != ''"> and dept_id = #{deptId}</if>
        </where>
    </select>
    
    <select id="selectTSurveySoilPollutionById" parameterType="Long" resultMap="TSurveySoilPollutionResult">
        <include refid="selectTSurveySoilPollutionVo"/>
        where id = #{id}
    </select>

    <insert id="insertTSurveySoilPollution" parameterType="TSurveySoilPollution" useGeneratedKeys="true" keyProperty="id">
        insert into t_survey_soil_pollution
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="surveyAssessmentId != null">survey_assessment_id,</if>
            <if test="pollutantName != null">pollutant_name,</if>
            <if test="maxConcentration != null">max_concentration,</if>
            <if test="deptId != null">dept_id,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="surveyAssessmentId != null">#{surveyAssessmentId},</if>
            <if test="pollutantName != null">#{pollutantName},</if>
            <if test="maxConcentration != null">#{maxConcentration},</if>
            <if test="deptId != null">#{deptId},</if>
         </trim>
    </insert>

    <update id="updateTSurveySoilPollution" parameterType="TSurveySoilPollution">
        update t_survey_soil_pollution
        <trim prefix="SET" suffixOverrides=",">
            <if test="surveyAssessmentId != null">survey_assessment_id = #{surveyAssessmentId},</if>
            <if test="pollutantName != null">pollutant_name = #{pollutantName},</if>
            <if test="maxConcentration != null">max_concentration = #{maxConcentration},</if>
            <if test="deptId != null">dept_id = #{deptId},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteTSurveySoilPollutionById" parameterType="Long">
        delete from t_survey_soil_pollution where id = #{id}
    </delete>

    <delete id="deleteTSurveySoilPollutionByIds" parameterType="String">
        delete from t_survey_soil_pollution where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>